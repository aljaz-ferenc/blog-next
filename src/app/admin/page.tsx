"use client";

import { useFormState } from "react-dom";
import * as actions from "../actions";

export default function AdminPage() {
  return (
    <div>
      <input type="password" />
    </div>
  );
}
